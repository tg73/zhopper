
terminal 1:

(create probe test gcode)


./gcode_response_spy.py --host ratos2.local| tee ~/tmp/ratos3.out

(start test print for probe accuracy, wait to finish)

terminal 2:
tail -f -c +0 ~/tmp/ratos3.out |grep --line-buffered "// Result is z="|./convert2_to_influx.py| influx write --bucket huihai
